//Add color picker

jQuery(document).ready(function($){
    $('.meta-survey-color').wpColorPicker();
    $('.meta-survey-color-hover').wpColorPicker();
    $('.meta-survey-button-color').wpColorPicker();
    $('.meta-survey-button-color-hover').wpColorPicker();
    $('.meta-survey-progress-bar-color').wpColorPicker();
    //$('.meta-survey-header-color').wpColorPicker();
    // $('.meta-survey-header-bgcolour').wpColorPicker();
});